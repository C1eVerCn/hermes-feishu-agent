# Plan B — 飞书卡片渲染 + 真回调 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. **前置：Plan A 已完成并全绿。**

**Goal:** 用 hermes `post_tool_call` 钩子捕获工具返回的原始 JSON，确定性地渲染成飞书互动卡片（无标题栏、摘要块 + 确定性数据块 + 交互按钮），并实现按钮真回调链路（点击 → 注入身份 → OCL 角色门控 → 直接调 API → 更新卡片 + toast）。

**Architecture:** 新增 `ocl/tool_capture.py`（按 session 累积本轮工具结果，带锁）；feishu_acl 插件追加 `post_tool_call` 钩子写入 capture；新增 `ocl/markdown_to_lark.py`（markdown→lark_md）与 `ocl/card_builder.py`（组装卡片）；`OclResult` 增加 `card` 字段，`pipeline.apply()` 末尾生成卡片；`feishu/sender.py` 增 `send_card`；`feishu/ws_client.py` 注册卡片 action 事件；新增 `bot/card_action_handler.py`。

**Tech Stack:** Python 3.11, lark-oapi（互动卡片 + card action callback）, pytest。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `ocl/tool_capture.py` | 创建 | 按 session 累积/读取/清空本轮工具结果（带锁） |
| `hermes_plugins/feishu_acl/__init__.py` | 修改 | 追加注册 `post_tool_call` 钩子 → tool_capture.record |
| `ocl/markdown_to_lark.py` | 创建 | markdown（粗体/列表/标题/代码块）→ lark_md 文本 + 元素 |
| `ocl/card_builder.py` | 创建 | 组装互动卡片：摘要块 + 确定性数据块 + 交互按钮 |
| `ocl/pipeline.py` | 修改 | `OclResult.card`；apply() 末尾调 card_builder |
| `feishu/sender.py` | 修改 | 新增 `send_card(chat_id, card)` |
| `feishu/ws_client.py` | 修改 | 注册 card action 事件处理 |
| `bot/card_action_handler.py` | 创建 | 卡片回调：注入身份→OCL→调工具→更新卡片+toast |
| `bot/handler.py` | 修改 | chat 前 clear capture、chat 后取 capture 传入 pipeline；按 card/text 分发 |
| `tests/unit/test_*` | 新增 | 各模块单测 |

---

## Task B1: 工具结果捕获存储（tool_capture）

**Files:**
- Create: `ocl/tool_capture.py`
- Test: `tests/unit/test_tool_capture.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/unit/test_tool_capture.py
import threading
import ocl.tool_capture as tc


def test_record_then_read_in_order():
    tc.clear("s1")
    tc.record("s1", "list_my_reservations", {"code": 200, "data": [{"benchNo": "TJ001"}]})
    tc.record("s1", "list_architectures", {"code": 200, "data": ["1.0架构"]})
    items = tc.read("s1")
    assert [i["tool"] for i in items] == ["list_my_reservations", "list_architectures"]
    assert items[0]["result"]["data"][0]["benchNo"] == "TJ001"


def test_sessions_isolated():
    tc.clear("a"); tc.clear("b")
    tc.record("a", "t", {"code": 200})
    assert tc.read("b") == []


def test_clear_empties_session():
    tc.record("s2", "t", {"code": 200})
    tc.clear("s2")
    assert tc.read("s2") == []


def test_record_tolerates_non_json_string():
    tc.clear("s3")
    tc.record("s3", "t", "not json")
    assert tc.read("s3")[0]["result"] == "not json"


def test_thread_safe_concurrent_record():
    tc.clear("s4")
    def worker():
        for _ in range(50):
            tc.record("s4", "t", {"code": 200})
    threads = [threading.Thread(target=worker) for _ in range(4)]
    for t in threads: t.start()
    for t in threads: t.join()
    assert len(tc.read("s4")) == 200
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_tool_capture.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# ocl/tool_capture.py
"""Per-session capture of this turn's tool results, written by the feishu_acl
post_tool_call hook and read by bot/handler.py after agent.chat().

Keyed by hermes session_id. handler clears before chat and reads after.
Stores raw result (parsed dict when the tool returned a JSON string, else the
raw value) so card_builder can render deterministically."""
import json
import threading
from typing import Any

_lock = threading.Lock()
_store: dict[str, list[dict]] = {}


def _coerce(result: Any):
    if isinstance(result, str):
        try:
            return json.loads(result)
        except (json.JSONDecodeError, ValueError):
            return result
    return result


def record(session_id: str, tool_name: str, result: Any) -> None:
    entry = {"tool": tool_name, "result": _coerce(result)}
    with _lock:
        _store.setdefault(session_id, []).append(entry)


def read(session_id: str) -> list[dict]:
    with _lock:
        return list(_store.get(session_id, []))


def clear(session_id: str) -> None:
    with _lock:
        _store.pop(session_id, None)
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_tool_capture.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add ocl/tool_capture.py tests/unit/test_tool_capture.py
git commit -m "feat(ocl): per-session tool-result capture store"
```

---

## Task B2: feishu_acl 插件追加 post_tool_call 钩子

**Files:**
- Modify: `hermes_plugins/feishu_acl/__init__.py`
- Modify: `tests/unit/test_plugin_feishu_acl.py`（加 post hook 测试）

- [ ] **Step 1: 追加失败测试**

```python
# 追加到 tests/unit/test_plugin_feishu_acl.py
import hermes_plugins.feishu_acl as acl
import ocl.tool_capture as tc


def test_post_tool_call_records_into_capture():
    tc.clear("feishu_ou_x")
    acl._on_post_tool_call(
        tool_name="list_my_reservations",
        args={}, result='{"code":200,"data":[]}',
        session_id="feishu_ou_x", tool_call_id="c1",
    )
    items = tc.read("feishu_ou_x")
    assert len(items) == 1
    assert items[0]["tool"] == "list_my_reservations"
    assert items[0]["result"]["code"] == 200


def test_post_tool_call_no_session_is_noop():
    # must not raise
    acl._on_post_tool_call(tool_name="t", args={}, result="{}", session_id="")


def test_register_registers_both_hooks():
    registered = []
    class Ctx:
        def register_hook(self, name, cb):
            registered.append(name)
    acl.register(Ctx())
    assert "pre_tool_call" in registered
    assert "post_tool_call" in registered
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_plugin_feishu_acl.py -v`
Expected: FAIL（`_on_post_tool_call` 未定义 / register 只注册 pre）

- [ ] **Step 3: 实现**

在 `hermes_plugins/feishu_acl/__init__.py` 顶部加 `from ocl import tool_capture`，并新增：

```python
def _on_post_tool_call(
    tool_name: str = "",
    args=None,
    result=None,
    task_id: str = "",
    session_id: str = "",
    tool_call_id: str = "",
    **kwargs,
) -> None:
    """Observational: capture each tool's raw result for deterministic card
    rendering. Keyed by session_id. Fail-open — never raise into hermes."""
    if not session_id:
        return
    try:
        tool_capture.record(session_id, tool_name, result)
    except Exception:
        logger.exception("feishu_acl: capture failed tool=%s session=%s", tool_name, session_id)
```

并把 `register` 改为同时注册两个钩子：

```python
def register(ctx) -> None:
    ctx.register_hook("pre_tool_call", _on_pre_tool_call)
    ctx.register_hook("post_tool_call", _on_post_tool_call)
    logger.info("feishu_acl plugin registered: pre_tool_call + post_tool_call hooks active")
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_plugin_feishu_acl.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add hermes_plugins/feishu_acl/__init__.py tests/unit/test_plugin_feishu_acl.py
git commit -m "feat(plugin): capture tool results via post_tool_call hook"
```

---

## Task B3: markdown → lark_md

**Files:**
- Create: `ocl/markdown_to_lark.py`
- Test: `tests/unit/test_markdown_to_lark.py`

飞书 `lark_md` 支持 `**粗体**`、`-`/数字列表（保留即可，飞书会渲染）、`[text](url)`，
但**不支持** `#` 标题与 ```代码块```。本模块做最小转换：标题行 `#{1,6} X` → `**X**`，
代码块去掉围栏保留内容、整体加粗标识，其余原样（飞书 lark_md 已能渲染粗体/列表）。

- [ ] **Step 1: 写失败测试**

```python
# tests/unit/test_markdown_to_lark.py
from ocl.markdown_to_lark import to_lark_md


def test_headers_become_bold():
    assert to_lark_md("# 标题") == "**标题**"
    assert to_lark_md("### 小标题") == "**小标题**"


def test_bold_preserved():
    assert "**粗**" in to_lark_md("这是 **粗** 字")


def test_bullets_preserved():
    out = to_lark_md("- a\n- b")
    assert "- a" in out and "- b" in out


def test_code_fence_stripped():
    out = to_lark_md("```\nx = 1\n```")
    assert "```" not in out
    assert "x = 1" in out


def test_multiple_blank_lines_collapsed():
    out = to_lark_md("a\n\n\n\nb")
    assert "\n\n\n" not in out
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_markdown_to_lark.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# ocl/markdown_to_lark.py
"""Convert a model's markdown into Feishu lark_md-friendly text.
lark_md renders **bold**, lists and links, but NOT '#' headers or ``` fences.
We map headers to bold lines and strip code fences."""
import re

_HEADER = re.compile(r'^\s{0,3}#{1,6}\s+(.*?)\s*#*\s*$', re.MULTILINE)
_FENCE = re.compile(r'```[^\n]*\n?')
_MULTI_BLANK = re.compile(r'\n{3,}')


def to_lark_md(text: str) -> str:
    text = _HEADER.sub(lambda m: f"**{m.group(1).strip()}**", text)
    text = _FENCE.sub("", text)
    text = _MULTI_BLANK.sub("\n\n", text)
    return text.strip()
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_markdown_to_lark.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add ocl/markdown_to_lark.py tests/unit/test_markdown_to_lark.py
git commit -m "feat(ocl): markdown → lark_md conversion"
```

---

## Task B4: 卡片构建器（card_builder）

**Files:**
- Create: `ocl/card_builder.py`
- Test: `tests/unit/test_card_builder.py`

卡片结构（**无 header 标题栏**）：
1. 摘要块：`{"tag":"div","text":{"tag":"lark_md","content": to_lark_md(text)}}`
2. 确定性数据块：若捕获到结构化查询，按工具类型渲染。
3. 交互按钮：见 spec §3.4 边界。

状态徽标用文字：`STATUS_BADGE = {0:"🟡待审批",1:"🟢已批准",2:"🔴已拒绝",3:"⚪已取消",4:"✅已完成"}`。

- [ ] **Step 1: 写失败测试**

```python
# tests/unit/test_card_builder.py
from ocl.card_builder import build_card


def _div_texts(card):
    out = []
    for el in card["elements"]:
        if el.get("tag") == "div" and "text" in el:
            out.append(el["text"]["content"])
    return "\n".join(out)


def test_no_header():
    card = build_card("你好", [])
    assert "header" not in card


def test_summary_block_present():
    card = build_card("# 结果\n**好的**", [])
    assert "**结果**" in _div_texts(card)
    assert "**好的**" in _div_texts(card)


def test_reservations_data_block_renders_fields():
    captured = [{"tool": "list_my_reservations", "result": {"code": 200, "data": [
        {"benchNo": "TJ001", "startTime": "2099-01-01 09:00:00", "endTime": "2099-01-01 10:00:00",
         "taskName": "性能测试", "status": 0, "statusDesc": "待审批"}]}}]
    card = build_card("您有1条预约", captured)
    text = _div_texts(card)
    assert "TJ001" in text
    assert "性能测试" in text
    assert "待审批" in text


def test_pending_reservation_gets_cancel_button():
    captured = [{"tool": "list_my_reservations", "result": {"code": 200, "data": [
        {"benchNo": "TJ001", "startTime": "2099-01-01 09:00:00", "endTime": "2099-01-01 10:00:00",
         "taskName": "t", "status": 0, "statusDesc": "待审批"}]}}]
    card = build_card("x", captured)
    actions = [e for e in card["elements"] if e.get("tag") == "action"]
    assert actions, "expected an action block with a cancel button"
    btn = actions[0]["actions"][0]
    assert btn["value"]["action"] == "cancel"
    assert btn["value"]["benchNo"] == "TJ001"


def test_approved_reservation_gets_return_button():
    captured = [{"tool": "list_my_reservations", "result": {"code": 200, "data": [
        {"benchNo": "TJ006", "startTime": "2099-03-01 09:00:00", "endTime": "2099-03-01 12:00:00",
         "taskName": "t", "status": 1, "statusDesc": "已批准"}]}}]
    card = build_card("x", captured)
    values = [a["value"]["action"] for e in card["elements"] if e.get("tag") == "action" for a in e["actions"]]
    assert "return" in values


def test_approvals_pending_gets_approve_reject_buttons():
    captured = [{"tool": "list_my_approvals", "result": {"code": 200, "data": [
        {"benchNo": "TJ002", "startTime": "2099-01-01 09:00:00", "endTime": "2099-01-01 10:00:00",
         "taskName": "t", "employeeName": "张三", "status": 0, "statusDesc": "待审批"}]}}]
    card = build_card("x", captured)
    values = [a["value"] for e in card["elements"] if e.get("tag") == "action" for a in e["actions"]]
    results = {v.get("approvalResult") for v in values if v["action"] == "approve"}
    assert results == {1, 2}


def test_available_benches_no_reserve_button():
    captured = [{"tool": "list_available_benches", "result": {"code": 200, "data": ["TJ002", "TJ004"]}}]
    card = build_card("可用台架有 TJ002、TJ004", captured)
    assert "TJ002" in _div_texts(card)
    actions = [e for e in card["elements"] if e.get("tag") == "action"]
    assert actions == []  # 预约按钮参数不自足，不放


def test_architectures_rendered_as_list():
    captured = [{"tool": "list_architectures", "result": {"code": 200, "data": ["1.0架构", "L3架构"]}}]
    card = build_card("支持以下架构", captured)
    text = _div_texts(card)
    assert "1.0架构" in text and "L3架构" in text


def test_error_result_no_data_block_no_buttons():
    captured = [{"tool": "reserve_bench", "result": {"error": "HTTP 400: 台架不存在"}}]
    card = build_card("预约失败：台架不存在", captured)
    actions = [e for e in card["elements"] if e.get("tag") == "action"]
    assert actions == []
    assert "预约失败" in _div_texts(card)
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_card_builder.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# ocl/card_builder.py
"""Build a Feishu interactive card (no header) from the model's text plus the
captured raw tool results. Deterministic data blocks come straight from the API
JSON; interactive buttons are added only for parameter-complete actions."""
from ocl.markdown_to_lark import to_lark_md

STATUS_BADGE = {0: "🟡待审批", 1: "🟢已批准", 2: "🔴已拒绝", 3: "⚪已取消", 4: "✅已完成"}

# tools whose data we render deterministically
_LIST_RESERVATION_TOOLS = ("list_my_reservations",)
_LIST_APPROVAL_TOOLS = ("list_my_approvals",)
_LIST_BENCH_TOOLS = ("list_available_benches",)
_LIST_ARCH_TOOLS = ("list_architectures",)


def _div(content: str) -> dict:
    return {"tag": "div", "text": {"tag": "lark_md", "content": content}}


def _hr() -> dict:
    return {"tag": "hr"}


def _button(text: str, value: dict, btype: str = "default") -> dict:
    return {"tag": "button", "text": {"tag": "plain_text", "content": text},
            "type": btype, "value": value}


def _action(buttons: list[dict]) -> dict:
    return {"tag": "action", "actions": buttons}


def _last_structured(captured: list[dict]):
    """Return the last successful structured query result, or None."""
    for entry in reversed(captured):
        res = entry.get("result")
        if not isinstance(res, dict) or res.get("code") != 200:
            continue
        if entry["tool"] in (_LIST_RESERVATION_TOOLS + _LIST_APPROVAL_TOOLS
                             + _LIST_BENCH_TOOLS + _LIST_ARCH_TOOLS):
            return entry
    return None


def _reservation_field(r: dict) -> str:
    badge = STATUS_BADGE.get(r.get("status"), r.get("statusDesc", ""))
    lines = [f"**{r.get('taskName', '(无任务名)')}**  {badge}",
             f"台架编号：{r.get('benchNo', '')}",
             f"时间：{r.get('startTime', '')} 至 {r.get('endTime', '')}"]
    if r.get("employeeName"):
        lines.append(f"预约人：{r['employeeName']}")
    if r.get("testPurpose"):
        lines.append(f"测试目的：{r['testPurpose']}")
    if r.get("reviewerName"):
        lines.append(f"审批人：{r['reviewerName']}")
    return "\n".join(lines)


def build_card(text: str, captured: list[dict]) -> dict:
    elements: list[dict] = [_div(to_lark_md(text))]
    entry = _last_structured(captured)

    if entry is not None:
        tool = entry["tool"]
        data = entry["result"].get("data") or []
        elements.append(_hr())

        if tool in _LIST_RESERVATION_TOOLS or tool in _LIST_APPROVAL_TOOLS:
            for r in data:
                elements.append(_div(_reservation_field(r)))
                buttons = _buttons_for(tool, r)
                if buttons:
                    elements.append(_action(buttons))
        elif tool in _LIST_BENCH_TOOLS:
            if data:
                elements.append(_div("可用台架：\n" + "\n".join(f"- {b}" for b in data)))
            else:
                elements.append(_div("当前没有可用台架。"))
        elif tool in _LIST_ARCH_TOOLS:
            elements.append(_div("支持的架构：\n" + "\n".join(f"- {a}" for a in data)))

    return {"config": {"wide_screen_mode": True}, "elements": elements}


def _buttons_for(tool: str, r: dict) -> list[dict]:
    bench_no = r.get("benchNo")
    start, end = r.get("startTime"), r.get("endTime")
    status = r.get("status")
    if tool in _LIST_RESERVATION_TOOLS:
        if status == 0:
            return [_button("取消预约",
                            {"action": "cancel", "benchNo": bench_no, "startTime": start, "endTime": end},
                            "danger")]
        if status == 1:
            return [_button("归还台架",
                            {"action": "return", "benchNo": bench_no})]
        return []
    if tool in _LIST_APPROVAL_TOOLS:
        if status == 0:
            return [
                _button("批准", {"action": "approve", "benchNo": bench_no, "approvalResult": 1,
                                "startTime": start, "endTime": end}, "primary"),
                _button("拒绝", {"action": "approve", "benchNo": bench_no, "approvalResult": 2,
                                "startTime": start, "endTime": end}, "danger"),
            ]
        return []
    return []
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_card_builder.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: 提交**

```bash
git add ocl/card_builder.py tests/unit/test_card_builder.py
git commit -m "feat(ocl): interactive card builder (summary + data block + buttons)"
```

---

## Task B5: pipeline 接入卡片

**Files:**
- Modify: `ocl/pipeline.py`
- Modify: `tests/unit/test_ocl_pipeline.py`

- [ ] **Step 1: 追加失败测试**

```python
# 追加到 tests/unit/test_ocl_pipeline.py
from ocl.pipeline import apply


def test_apply_returns_card_with_captured():
    captured = [{"tool": "list_my_reservations", "result": {"code": 200, "data": [
        {"benchNo": "TJ001", "startTime": "2099-01-01 09:00:00", "endTime": "2099-01-01 10:00:00",
         "taskName": "t", "status": 0, "statusDesc": "待审批"}]}}]
    res = apply("您有1条预约", "ou_x", captured=captured)
    assert res.card is not None
    assert any(e.get("tag") == "action" for e in res.card["elements"])


def test_apply_blocked_has_no_card():
    res = apply("", "ou_x", captured=[])  # empty → blocked
    assert res.blocked is True
    assert res.card is None


def test_apply_plain_text_still_builds_card_without_data_block():
    res = apply("**你好**", "ou_x", captured=[])
    assert res.card is not None
    # only the summary div, no action/hr
    assert all(e.get("tag") != "action" for e in res.card["elements"])
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_ocl_pipeline.py -v`
Expected: FAIL（`apply` 不接受 `captured` / `OclResult` 无 `card`）

- [ ] **Step 3: 实现**

修改 `ocl/pipeline.py`：

```python
from ocl import format_control, content_filter, length_limiter, card_builder
...

@dataclass
class OclResult:
    text: str
    blocked: bool
    block_reason: str = ""
    card: dict | None = None


def apply(response: str, user_id: str, captured: list[dict] | None = None) -> OclResult:
    captured = captured or []
    try:
        fmt = format_control.apply(response)
        if fmt.blocked:
            return OclResult(text=_EMPTY_MESSAGE, blocked=True, block_reason=fmt.block_reason, card=None)
        text = fmt.text

        content = content_filter.check(text)
        if content.blocked:
            block_msg = getattr(settings, "OCL_CONTENT_BLOCK_MESSAGE", _BLOCK_MESSAGE)
            return OclResult(text=block_msg, blocked=True, block_reason=content.reason, card=None)

        text = length_limiter.apply(text)
        card = card_builder.build_card(text, captured)
        return OclResult(text=text, blocked=False, card=card)

    except Exception:
        log.exception("ocl_pipeline_error user_id=%s — failing open", user_id)
        return OclResult(text=response, blocked=False, block_reason="", card=None)
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_ocl_pipeline.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add ocl/pipeline.py tests/unit/test_ocl_pipeline.py
git commit -m "feat(ocl): pipeline emits interactive card alongside text"
```

---

## Task B6: sender.send_card

**Files:**
- Modify: `feishu/sender.py`
- Modify: `tests/unit/test_sender.py`

- [ ] **Step 1: 追加失败测试**

```python
# 追加到 tests/unit/test_sender.py
import json
from unittest.mock import patch, MagicMock
import feishu.sender as sender


def test_send_card_builds_interactive_payload():
    card = {"config": {"wide_screen_mode": True}, "elements": [{"tag": "div"}]}
    fake_resp = MagicMock(); fake_resp.success.return_value = True
    with patch.object(sender._client.im.v1.message, "create", return_value=fake_resp) as create:
        sender.send_card("oc_chat", card)
        req = create.call_args[0][0]
        body = req.request_body
        assert body.msg_type == "interactive"
        # content is JSON string of the card
        assert json.loads(body.content)["elements"][0]["tag"] == "div"
```

> 注：lark-oapi 的 builder 字段访问以实际版本为准；若 `request_body` 内部属性名不同，
> 实现前先 `python -c "from lark_oapi.api.im.v1 import CreateMessageRequestBody; print(dir(CreateMessageRequestBody))"` 核对，必要时调整断言为对 `create` 被调用且 msg_type 正确的较松断言。

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_sender.py -v`
Expected: FAIL（`send_card` 未定义）

- [ ] **Step 3: 实现（在 sender.py 追加）**

```python
def send_card(chat_id: str, card: dict, max_retries: int = 3) -> None:
    """Send an interactive card to a chat. Rate-limited like text sends."""
    global _last_send_time
    with _send_lock:
        elapsed = time.monotonic() - _last_send_time
        if elapsed < RATE_LIMIT_INTERVAL:
            time.sleep(RATE_LIMIT_INTERVAL - elapsed)
        _last_send_time = time.monotonic()

    content = json.dumps(card, ensure_ascii=False)
    for attempt in range(max_retries):
        req = CreateMessageRequest.builder() \
            .receive_id_type("chat_id") \
            .request_body(
                CreateMessageRequestBody.builder()
                .receive_id(chat_id)
                .msg_type("interactive")
                .content(content)
                .build()
            ).build()
        resp = _client.im.v1.message.create(req)
        if resp.success():
            metrics.inc("cards_sent")
            return
        if resp.code == 429:
            time.sleep(2 ** attempt)
            continue
        log.error("Feishu send_card failed: code=%s msg=%s", resp.code, resp.msg)
        return
    log.error("Feishu send_card failed after %d retries for chat_id=%s", max_retries, chat_id)
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_sender.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add feishu/sender.py tests/unit/test_sender.py
git commit -m "feat(feishu): send_card for interactive messages"
```

---

## Task B7: handler 接入卡片发送

**Files:**
- Modify: `bot/handler.py`
- Modify: `tests/unit/test_handler_intent.py`

- [ ] **Step 1: 追加失败测试**

```python
# 追加到 tests/unit/test_handler_intent.py
from unittest.mock import patch, MagicMock


def test_handle_clears_and_reads_capture_and_sends_card(monkeypatch):
    import bot.handler as handler
    sent = {}
    monkeypatch.setattr(handler.sender, "send_card", lambda chat, card: sent.update(card=card))
    monkeypatch.setattr(handler.sender, "send", lambda chat, text: sent.update(text=text))
    monkeypatch.setattr(handler.identity, "email_of", lambda uid: "zhangsan@example.com")

    fake_agent = MagicMock(); fake_agent.chat.return_value = "您有1条预约"
    monkeypatch.setattr(handler.agent_pool, "get_or_create", lambda uid: fake_agent)
    monkeypatch.setattr(handler.tool_capture, "read",
                        lambda sid: [{"tool": "list_my_reservations", "result": {"code": 200, "data": [
                            {"benchNo": "TJ001", "startTime": "2099-01-01 09:00:00",
                             "endTime": "2099-01-01 10:00:00", "taskName": "t",
                             "status": 0, "statusDesc": "待审批"}]}}])

    data = _make_message_event("帮我看我的预约", "ou_x", "oc_chat")  # helper in this test file
    handler._handle(data)
    assert "card" in sent  # card path taken
```

> 若测试文件无 `_make_message_event` 辅助，先添加一个构造最小 `P2ImMessageReceiveV1`
> 替身的辅助函数（用 `types.SimpleNamespace` 搭出 `.event.message`/`.event.sender`）。

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_handler_intent.py -v`
Expected: FAIL

- [ ] **Step 3: 实现**

在 `bot/handler.py`：
- `from ocl import tool_capture`。
- 在 agent 调用段：用稳定 `session_id = f"feishu_{user_id}"`（与 agent_pool 一致）。
  chat 前 `tool_capture.clear(session_id)`；chat 后 `captured = tool_capture.read(session_id)`。
- OCL 调用改为 `ocl_result = ocl_apply(response or "", user_id, captured=captured)`。
- 发送分发：

```python
    if ocl_result.card is not None and not ocl_result.blocked:
        try:
            sender.send_card(chat_id, ocl_result.card)
        except Exception:
            log.exception("send_card failed, falling back to text")
            sender.send(chat_id, ocl_result.text or _ERROR_REPLY)
    else:
        sender.send(chat_id, ocl_result.text or _ERROR_REPLY)
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_handler_intent.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add bot/handler.py tests/unit/test_handler_intent.py
git commit -m "feat(bot): capture tool results per turn and send interactive card"
```

---

## Task B8: 卡片回调处理器（card_action_handler）

**Files:**
- Create: `bot/card_action_handler.py`
- Test: `tests/unit/test_card_action_handler.py`

职责：接收 `(open_id, value)` → 注入 email + OCL 角色门控 → 调对应工具（直接用
`mock_tools.handlers`，经 `set_current_email`）→ 返回 `(toast_text, updated_card)`。

`action → (tool_name, handler)` 映射：
- `cancel`  → `cancel_reservation`
- `approve` → `approve_reservation`
- `return`  → `return_bench`（缺 returnLocation：走「后续消息」语义，返回提示 toast +
  不更新数据，由上层引导用户补地点；MVP 在 handler 内返回 need_followup 标志）

- [ ] **Step 1: 写失败测试**

```python
# tests/unit/test_card_action_handler.py
import json
from unittest.mock import patch
import pytest

import bot.card_action_handler as cah
import ocl.identity as identity


@pytest.fixture(autouse=True)
def _ident(tmp_path, monkeypatch):
    f = tmp_path / "identity_map.json"
    f.write_text(json.dumps({
        "ou_user":  {"email": "zhangsan@example.com", "name": "张三", "role": 1},
        "ou_sched": {"email": "scheduler1@example.com", "name": "调度员1", "role": 2},
    }, ensure_ascii=False))
    monkeypatch.setattr(identity, "_MAP_FILE", str(f))
    identity._invalidate_cache()
    yield


def test_cancel_calls_handler_and_returns_toast():
    with patch.object(cah.handlers, "cancel_reservation",
                      return_value='{"code":200,"message":"取消预约成功","data":null}') as m:
        toast, card = cah.handle("ou_user", {"action": "cancel", "benchNo": "TJ001",
                                             "startTime": "2099-01-01 09:00:00",
                                             "endTime": "2099-01-01 10:00:00"})
        assert "取消" in toast
        m.assert_called_once()


def test_approve_denied_for_normal_user_via_ocl():
    with patch.object(cah.handlers, "approve_reservation") as m:
        toast, card = cah.handle("ou_user", {"action": "approve", "benchNo": "TJ001",
                                             "approvalResult": 1})
        assert "权限" in toast
        m.assert_not_called()


def test_approve_allowed_for_scheduler():
    with patch.object(cah.handlers, "approve_reservation",
                      return_value='{"code":200,"message":"审批成功:批准1条预约记录","data":null}') as m:
        toast, card = cah.handle("ou_sched", {"action": "approve", "benchNo": "TJ001",
                                             "approvalResult": 1})
        assert "审批" in toast or "批准" in toast
        m.assert_called_once()


def test_unknown_user_rejected():
    toast, card = cah.handle("ou_ghost", {"action": "cancel", "benchNo": "TJ001"})
    assert "平台用户" in toast


def test_api_business_error_surfaced_in_toast():
    with patch.object(cah.handlers, "cancel_reservation",
                      return_value='{"error":"HTTP 400: 未找到待审批状态的预约记录"}'):
        toast, card = cah.handle("ou_user", {"action": "cancel", "benchNo": "TJ001"})
        assert "未找到" in toast or "失败" in toast


def test_return_without_location_requests_followup():
    toast, card = cah.handle("ou_user", {"action": "return", "benchNo": "TJ006"})
    assert "地点" in toast  # asks user to provide return location
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_card_action_handler.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# bot/card_action_handler.py
"""Handle Feishu interactive-card button callbacks.
Deterministic (no LLM): inject identity → OCL role gate → call the tool handler.
Returns (toast_text, updated_card_or_None). value never carries email — the
clicker's open_id resolves it, so a forged value cannot impersonate."""
import json
import logging

from ocl import identity, permission
from ocl.tool_guard import set_current_email, set_current_user
from mock_tools import handlers

log = logging.getLogger(__name__)

# action → (tool registry name, handler callable)
_ACTION_TOOL = {
    "cancel":  ("cancel_reservation",  handlers.cancel_reservation),
    "approve": ("approve_reservation", handlers.approve_reservation),
    "return":  ("return_bench",        handlers.return_bench),
}


def handle(open_id: str, value: dict) -> tuple[str, dict | None]:
    action = value.get("action", "")
    email = identity.email_of(open_id)
    if not email:
        return "您还不是平台用户，请联系管理员开通。", None

    if action == "return" and not value.get("returnLocation"):
        bench = value.get("benchNo", "")
        return f"请在对话中告诉我 {bench} 的还台地点，例如「归还 {bench}，地点安亭广场」。", None

    pair = _ACTION_TOOL.get(action)
    if pair is None:
        return "暂不支持该操作。", None
    tool_name, fn = pair

    if not permission.is_tool_permitted(open_id, tool_name):
        return "权限不足：该操作需要更高角色。", None

    args = {k: v for k, v in value.items() if k != "action"}
    set_current_user(open_id)
    set_current_email(email)
    try:
        raw = fn(args)
    finally:
        set_current_user("")
        set_current_email("")

    try:
        parsed = json.loads(raw)
    except (json.JSONDecodeError, ValueError):
        parsed = {"error": raw}

    if "error" in parsed:
        detail = parsed["error"].split(":", 1)[-1].strip()
        return f"操作失败：{detail}", None
    if parsed.get("code") == 200:
        return parsed.get("message", "操作成功"), None
    return parsed.get("message", "操作失败"), None
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_card_action_handler.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: 提交**

```bash
git add bot/card_action_handler.py tests/unit/test_card_action_handler.py
git commit -m "feat(bot): deterministic card-callback handler with OCL gate"
```

---

## Task B9: ws_client 注册卡片回调

**Files:**
- Modify: `feishu/ws_client.py`
- Test: `tests/unit/test_ws_card_action.py`

> **实现前必做**：核对当前 lark-oapi 卡片回调 API。运行：
> `python -c "import lark_oapi as lark; print([m for m in dir(lark.EventDispatcherHandler.builder(encrypt_key='',verification_token='')) if 'card' in m.lower()])"`
> 以及 `python -c "import lark_oapi; print([n for n in dir(lark_oapi) if 'Card' in n])"`。
> 预期存在 `register_p2_card_action_trigger_v1`（回调返回可含 toast/卡片）。
> 若该版本 API 名不同（如 `register_card_action`/`P2CardActionTrigger`），据实调整
> 下方 `_on_card_action` 的签名与返回类型；核心逻辑（调 card_action_handler.handle、
> 返回 toast）不变。

- [ ] **Step 1: 写失败测试（只测我们的回调薄封装，不测 lark 内部）**

```python
# tests/unit/test_ws_card_action.py
from unittest.mock import patch
import feishu.ws_client as ws


def test_on_card_action_delegates_to_handler_and_returns_toast():
    fake_data = _make_card_action("ou_user", {"action": "cancel", "benchNo": "TJ001"})
    with patch.object(ws.card_action_handler, "handle",
                      return_value=("取消预约成功", None)) as m:
        resp = ws._on_card_action(fake_data)
        m.assert_called_once()
        # response carries the toast text somewhere serialisable
        assert "取消预约成功" in ws._toast_text(resp)


def _make_card_action(open_id, value):
    import types
    return types.SimpleNamespace(
        event=types.SimpleNamespace(
            operator=types.SimpleNamespace(open_id=open_id),
            action=types.SimpleNamespace(value=value),
        )
    )
```

> 注：`_make_card_action` 的字段路径需与所选 lark-oapi 版本的 card action 事件结构对齐
> （Step 0 核对时一并确认 `event.operator.open_id` 与 `event.action.value` 的真实路径），
> 据实调整 helper 与 `_on_card_action` 的取值。`ws._toast_text(resp)` 为测试辅助，从
> 回调响应对象里提取 toast 文本；若响应是 dict 则直接取键。

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_ws_card_action.py -v`
Expected: FAIL（`_on_card_action` 未定义）

- [ ] **Step 3: 实现**

在 `feishu/ws_client.py`：
- `from bot import card_action_handler`。
- 新增回调（以核对到的 lark-oapi API 为准，下例假设 `register_p2_card_action_trigger_v1`
  且回调返回 dict 形式的 toast）：

```python
def _extract_card_action(data):
    """Pull (open_id, value) from a lark card action event. Path verified in Step 0."""
    op = data.event.operator
    open_id = getattr(op, "open_id", "") or ""
    value = getattr(data.event.action, "value", {}) or {}
    return open_id, value


def _on_card_action(data):
    """Synchronous card callback: run the deterministic action, return a toast."""
    try:
        open_id, value = _extract_card_action(data)
        toast_text, updated_card = card_action_handler.handle(open_id, value)
    except Exception:
        log.exception("card action handling failed")
        toast_text, updated_card = "操作处理失败，请稍后重试", None
    return _build_card_action_response(toast_text, updated_card)


def _build_card_action_response(toast_text, updated_card):
    """Build the lark callback response (toast + optional card update).
    Exact response type per lark-oapi version verified in Step 0."""
    resp = {"toast": {"type": "info", "content": toast_text}}
    if updated_card is not None:
        resp["card"] = {"type": "raw", "data": updated_card}
    return resp


def _toast_text(resp) -> str:
    if isinstance(resp, dict):
        return resp.get("toast", {}).get("content", "")
    return getattr(getattr(resp, "toast", None), "content", "") or ""
```

- 在 `_build_client()` 的 `EventDispatcherHandler.builder(...)` 链上追加
  `.register_p2_card_action_trigger_v1(_on_card_action)`（名称以 Step 0 核对为准）。

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_ws_card_action.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add feishu/ws_client.py tests/unit/test_ws_card_action.py
git commit -m "feat(feishu): register card action callback → deterministic handler"
```

---

## Task B10: 文档同步 + 全量回归

**Files:**
- Modify: `ocl/CLAUDE.md`, `feishu/CLAUDE.md`, `bot/CLAUDE.md`, `docs/architecture.md`

- [ ] **Step 1: 文档**

- `ocl/CLAUDE.md`：新增 `tool_capture.py`、`markdown_to_lark.py`、`card_builder.py` 条目；
  `pipeline.apply()` 签名更新为 `(response, user_id, captured=None) → OclResult(card=…)`；
  不变量补「card_builder 为纯 CPU，满足 <100ms」。
- `feishu/CLAUDE.md`：sender 新增 send_card；ws_client 新增 card action（说明回调是同步、
  需快速返回、不得阻塞 I/O）。
- `bot/CLAUDE.md`：新增 `card_action_handler.py` 流程；handler 流程图加 capture clear/read。
- `docs/architecture.md`：贴 spec §3.6 数据流全景。

- [ ] **Step 2: 全量回归**

Run: `pytest tests/unit/ -q && pytest tests/integration/test_mock_api.py -q`
Expected: 全绿。

- [ ] **Step 3: 手动验证清单（需飞书实例，记录到 docs）**

- 普通用户「查我的预约」→ 收到卡片，待审批项有「取消预约」按钮，点击 → toast「取消预约成功」。
- 调度员「我的审批」→ 待审批项有「批准/拒绝」按钮，点击批准 → toast「审批成功」。
- 普通用户点「批准」按钮（伪造场景）→ toast「权限不足」。
- 「归还 TJxxx」无地点 → 引导补地点。
- 纯问答（你好）→ 卡片仅摘要、无数据块/按钮。

- [ ] **Step 4: 提交**

```bash
git add ocl/CLAUDE.md feishu/CLAUDE.md bot/CLAUDE.md docs/
git commit -m "docs: card rendering + callback architecture"
```

---

## Self-Review（写计划后自查结论）

- **Spec 覆盖（第二部分）**：post_tool_call 捕获（B1/B2）✓；markdown→lark_md（B3）✓；
  无标题栏卡片+摘要+确定性数据块+按钮（B4）✓；pipeline 接卡片（B5）✓；send_card（B6）✓；
  handler 分发（B7）✓；真回调链路（B8/B9）✓；按钮边界（B4：available 无按钮、return 走
  后续消息）✓；OCL 角色 + API 业务两道闸（B8 测试覆盖）✓；文档（B10）✓。
- **类型/命名一致**：`tool_capture.record/read/clear` 跨 B1/B2/B7 一致；
  `build_card(text, captured)` 跨 B4/B5 一致；`OclResult.card` 跨 B5/B7 一致；
  action→tool 映射（cancel/approve/return）跨 B4 按钮 value 与 B8 `_ACTION_TOOL` 一致；
  按钮 value 字段（benchNo/startTime/endTime/approvalResult）跨 B4/B8 一致。
- **外部不确定点（已在计划内显式核对）**：lark-oapi 卡片回调 API 名与事件结构
  （B9 Step 0 实测命令 + fallback 说明）；`CreateMessageRequestBody` 字段（B6 注）。
- **占位扫描**：无 TODO/TBD；所有代码步均含完整代码；外部 API 不确定处给出实测命令
  与明确 fallback，而非「待定」。

---

**完成标志**：Plan A + Plan B 全部任务通过后，机器人在台架预约域端到端可用，回复以
无标题栏互动卡片呈现（摘要 + 确定性数据 + 交互按钮），按钮点击走确定性真回调，OCL
角色门控与 mock API 业务规则双重生效。
